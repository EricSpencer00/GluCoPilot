import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  header: {
    margin: 16,
    fontWeight: 'bold',
  },
  chartCard: {
    margin: 16,
  },
  cardTitle: {
    marginBottom: 8,
  },
});
